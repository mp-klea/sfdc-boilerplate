machine:
  environment:
    username: klea@1makepositive.ci.test	
    password: x
    serverurl: login.salesforce.com
    
test:
  override:
    - ant -f build/build.xml -lib build/lib/ test: 
        timeout: 600
